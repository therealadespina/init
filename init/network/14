# What file contains the local DNS entries?

echo /etc/hosts

# Основная цель DNS — это отображение доменных имен в IP адреса и наоборот — IP в DNS.
# Исторически, до появления доменной системы имен роль инструмента разрешения символьных имен в IP выполнял файл /etc/hosts (Более подробно https://habr.com/ru/post/137587/).
