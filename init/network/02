# identify and display the Ethernet interface characteristics:
# (a) Identify broadcast address
# (b) Identify all IP adresses which are part of the same subnet

ifconfig en0 | grep broadcast | awk '{print $6}'
ping -c 1 192.168.23.255
arp -a | awk '{print $2}' | sed 's/[^0-9.]//g'

# ifconfig - Используется для назначения сетевого адреса, изменение настроек параметров сетевого адаптера и IP протокола.
# grep - Утилита командной строки, которая находит на вводе строки, отвечающие заданному параметру (в моем случае это broadcast).
# awk '{print $2}' - выводит второй столбец.
# ping - в основном используется для проверки доступности удаленного хоста или нет. Удаленным хостом может быть веб-сервер, ваш маршрутизатор или система в вашей локальной сети.
# Как работает ping? Фактически он отправляет небольшие ICMP- пакеты на удаленный хост и ожидает ответа. Если хост включен, вы должны увидеть ответ.
# Это как технология сонара, используемая на подводных лодках. Подводные лодки посылают импульсы звукового сигнала и ждут возвращения эха.
# ping -c - отправить только N количество пакетов. При этом вы можете указать количество пакетов, которые команда ping должна отправить перед автоматической остановкой.
# arp - Утилита arp отображает и изменяет преобразование адресов Интернет-Ethernet таблицы, используемые протоколом разрешения адресов (arp (4)). Без флагов программа отображает текущую запись ARP для имени хоста. Хост может быть указан по имени или номер, используя интернет-точку.
# arp -a - Программа отображает или удаляет все текущие записи ARP. (Более подробно http://www.nsc.ru/win/docs/tcpip/arp.html)
# sed - Получает входной поток (обычно файл) построчно, редактирует каждую строку согласно правилам, определённым в sed-скрипте с использованием простого языка sed, и затем выводит результат в выходной поток.
# s/элементы//g - Удалить все найденные "элементы", оставляя остальную часть строки без именений.
# [ ] - Соответствуют одному любому из символов, включённых в квадратные скобки между [ и ].
# ^ - Соответствует началу строки.
# . - Соответствует любому единственному символу.
# [^ ] - Соответствуют одному любому из символов, не включённых в квадратные скобки между [^ и ].
