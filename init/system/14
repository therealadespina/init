# Run the ‘tail -f /var/log/syslog‘ command in background

tail -f /var/logsyslog &

# tail - Отображает содержимое файла или, по умолчанию, его стандартный ввод в стандартный вывод.
# -f - Заставляет tail не останавливаться при достижении конца файла, а скорее ждать добавления дополнительных данных к входным данным.
