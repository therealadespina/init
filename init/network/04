# Identifiy the default gateway in the routing table

route get default | grep gateway | cut -c14-26

# route - это Утилита, используемая для ручного управления таблицами сетевой маршрутизации.
# grep - Утилита grep ищет любые входные файлы, выбирая строки, соответствующие одному или большенству моделей.
# cut - то утилита командной строки, которая позволяет вырезать части строк из указанных файлов или переданных данных и выводить результат в стандартный вывод.
# При использовании команды Cut вы должны использовать один и только один из следующих параметров: (в моем случае -c(N-N) – Выберите, указав символ, набор символов или диапазон символов.)
